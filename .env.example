# Application Configuration
NODE_ENV=development
PORT=3000

# Backend API URLs (Local Development - Backend on host machine)
API_BASE_URL=http://localhost:8000
AUTH_API_BASE_URL=http://localhost:8000/api/auth

# Backend API URLs (Docker - Backend on host machine)
# Uncomment these for Docker when backend runs on your local machine
# API_BASE_URL=http://host.docker.internal:8000
# AUTH_API_BASE_URL=http://host.docker.internal:8000/api/auth

# Backend API URLs (Docker - Backend in separate container named "team2-backend")
# Uncomment these if backend is in another Docker container on same network
# API_BASE_URL=http://team2-backend:8000
# AUTH_API_BASE_URL=http://team2-backend:8000/api/auth

# Backend API URLs (Remote Server)
# Uncomment and update with your backend server URL
# API_BASE_URL=http://your-backend-server.com:8000
# AUTH_API_BASE_URL=http://your-backend-server.com:8000/api/auth

# Session Secret (REQUIRED - Generate with: openssl rand -hex 32)
# SESSION_SECRET=your-secret-key-min-32-characters-long
