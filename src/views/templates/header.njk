<nav class="bg-white/80 backdrop-blur-sm border-b border-slate-200/60 sticky top-0 z-50">
    <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
            <!-- Logo and Brand -->
            <div class="flex items-center space-x-3">
                <div class="flex items-center">
                    <img src="/KainosLogoNoBackground.png" alt="Kainos Logo" class="h-8 w-auto">
                </div>
                <div class="flex flex-col">
                    <p class="text-sm text-slate-600 font-medium">Thinking Without Limitations</p>
                </div>
            </div>
            
            <!-- Navigation -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="text-slate-700 hover:text-blue-600 font-medium transition-colors duration-200">Home</a>
                <a href="/job-roles" class="text-slate-700 hover:text-blue-600 font-medium transition-colors duration-200">Jobs</a>
                <a href="#about" class="text-slate-700 hover:text-blue-600 font-medium transition-colors duration-200">About</a>
                <a href="#contact" class="text-slate-700 hover:text-blue-600 font-medium transition-colors duration-200">Contact</a>
                
                <!-- Accessibility Button -->
                <button 
                    id="accessibility-btn"
                    class="flex items-center space-x-2 bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded-lg transition-all duration-200 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 shadow-sm"
                    aria-label="Accessibility options"
                >
                    <i data-lucide="accessibility" class="w-4 h-4"></i>
                    <span class="text-sm font-medium">Accessibility</span>
                </button>
            </div>
            
            <!-- Mobile Menu and Accessibility Buttons -->
            <div class="md:hidden flex items-center space-x-3">
                <!-- Mobile Accessibility Button -->
                <button 
                    id="accessibility-btn-mobile"
                    class="flex items-center bg-blue-600 hover:bg-blue-700 text-white p-2 rounded-lg transition-all duration-200 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
                    aria-label="Accessibility options"
                >
                    <i data-lucide="accessibility" class="w-5 h-5"></i>
                </button>
                
                <!-- Mobile Menu Button -->
                <button class="text-slate-700 hover:text-blue-600 transition-colors duration-200">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
</nav>