<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100 font-sans">
    <div class="max-w-4xl mx-auto p-8">
        <div class="bg-white p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold text-gray-900 border-b-2 border-blue-600 pb-2 mb-6">{{ title }}</h1>
            <div class="bg-gray-50 p-4 rounded-md mb-4">
                <p class="mb-2"><strong>Message:</strong> {{ message }}</p>
                <p class="mb-2"><strong>Application:</strong> <span class="text-blue-600 font-bold">{{ app }}</span></p>
                <p class="mb-2"><strong>Version:</strong> <span class="text-blue-600 font-bold">{{ version }}</span></p>
                <p class="mb-2"><strong>Environment:</strong> <span class="text-blue-600 font-bold">{{ environment }}</span></p>
                <p class="mb-0"><strong>Current Time:</strong> {{ timestamp }}</p>
            </div>
            <p class="text-lg mb-4">🎉 Successfully set up Nunjucks templating engine!</p>
            <p class="text-gray-700">This page is now rendered using Nunjucks instead of returning JSON.</p>
        </div>
    </div>
</body>
</html>